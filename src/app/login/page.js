import LoginForm from "@/components/LoginForm/LoginForm";

export const metadata = {
  title: "Sign in | Frontend One",
  description: "Sign in to manage your Backend-1 posts and gallery uploads.",
};

export default function LoginPage() {
  return <LoginForm />;
}
